# 이 파일은 비어 있다. 삭제해도 된다.
#
# 파이프라인 정의는 .github/workflows/cicd-ct.yml 로 이동했다.
# 이 경로에는 일시적으로만 있었다 (최초 푸시 시 토큰에 workflow 스코프가 없었음).
#
# 삭제 방법:
#   git rm ci/cicd-ct.yml .github/PERMISSION_TEST.md
#   git commit -m "chore: 이동 완료된 임시 파일 제거"
#   git push
#
# 편집하지 말 것 — 실제 파이프라인은 .github/workflows/cicd-ct.yml 이다.
